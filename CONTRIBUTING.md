# Contributing

Use the Chefe environment and keep changes focused.

```sh
chefe install
chefe run lint
chefe run typecheck
chefe run test
chefe run build
```

Update `README.md`, `SYSTEM.md`, and `CHANGELOG.md` when behavior changes.
