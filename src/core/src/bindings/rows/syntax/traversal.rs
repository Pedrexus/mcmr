pub(in crate::bindings) struct NodeTraversal {
    pub(in crate::bindings) ordinal: u64,
    pub(in crate::bindings) parent: Option<u64>,
    pub(in crate::bindings) depth: u64,
    pub(in crate::bindings) subtree_end: u64,
}
