/// Structural classification established from one class declaration.
pub(in crate::classes) struct ClassShape {
    pub(in crate::classes) is_plain: bool,
    pub(in crate::classes) is_declarative: bool,
}
