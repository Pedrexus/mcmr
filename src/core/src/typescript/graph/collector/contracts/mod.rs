pub(in crate::typescript::graph::collector) use callable::{CallableSignature, DeclaredParameter};
pub(in crate::typescript::graph::collector) use declaration::CallableDeclaration;
pub(in crate::typescript::graph::collector) use member::{KeyedMember, MemberBody};

mod callable;
mod declaration;
mod member;
